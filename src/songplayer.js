/* document.addEventListener('DOMContentLoaded', () => {
    const audio = new Audio('../public/bg-music.mp3');
    audio.muted = false;

    // Ensure audio is played by calling the function
    audio.play().catch((error) => {
        // Handle any play errors (such as autoplay restrictions)
        console.log("Autoplay prevented: " + error);
    });
});
 */
